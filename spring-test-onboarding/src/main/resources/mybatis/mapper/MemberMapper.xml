<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.testonboarding.member.dao.MemberDao">

    <sql id="memberColumns">
        member_id, username, password, nickname, role, created_at
    </sql>

    <select id="findById" resultType="Member">
        SELECT <include refid="memberColumns"/>
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <select id="findByUsername" resultType="Member">
        SELECT <include refid="memberColumns"/>
        FROM member
        WHERE username = #{username}
    </select>

    <!-- role 컬럼(NVARCHAR)은 MyBatis 기본 EnumTypeHandler가 Role enum과 이름으로 변환한다 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (username, password, nickname, role)
        VALUES (#{username}, #{password}, #{nickname}, #{role})
    </insert>

</mapper>
