<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webflow.order.dao.OrderDao">

    <sql id="orderColumns">
        order_id, product_id, quantity, total_price, status, payment_key, ordered_at
    </sql>

    <select id="findById" resultType="Order">
        SELECT <include refid="orderColumns"/>
        FROM orders
        WHERE order_id = #{orderId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO orders (product_id, quantity, total_price, status)
        VALUES (#{productId}, #{quantity}, #{totalPrice}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status},
            payment_key = #{paymentKey}
        WHERE order_id = #{orderId}
    </update>

</mapper>
